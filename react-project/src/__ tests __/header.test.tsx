// import '@testing-library/jest-dom';
// import Header from '../components/header/header';
// import { render, screen } from '@testing-library/react';

// jest.mock('../components/error-boundary/error-button', () => () => (
//   <div>ErrorButton Component</div>
// ));
// jest.mock('../redux/toggle-theme/theme-selector/theme-selector', () => () => (
//   <div>ThemeSelector Component</div>
// ));

// describe('Header', () => {
//   it('should render ErrorButton and ThemeSelector components', () => {
//     render(<Header />);

//     expect(screen.getByText('ErrorButton Component')).toBeInTheDocument();
//     expect(screen.getByText('ThemeSelector Component')).toBeInTheDocument();
//   });
// });
